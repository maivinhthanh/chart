export interface Props {
  data: Array<number>,
  color: String,
  svgHeight: number,
  svgWidth: number,
}

export interface PropsChart {
  data: Array<number> | String,
  color: String,
  mode: String
}

